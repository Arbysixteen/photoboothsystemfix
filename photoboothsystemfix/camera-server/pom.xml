<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.photobooth</groupId>
    <artifactId>camera-server</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Photobooth Camera Server</name>
    <description>HTTP microservice that wraps Canon EDSDK via canon-sdk-java for live view streaming and photo capture</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>11</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <!-- canon-sdk-java version (matches local clone) -->
        <canon-sdk-java.version>1.2.1-SNAPSHOT</canon-sdk-java.version>

        <!-- Dependencies -->
        <gson.version>2.10.1</gson.version>
        <slf4j.version>2.0.17</slf4j.version>
        <logback.version>1.5.27</logback.version>
    </properties>

    <dependencies>
        <!-- Canon SDK Java Framework (the high-level API) -->
        <dependency>
            <groupId>org.blackdread</groupId>
            <artifactId>camera-framework</artifactId>
            <version>${canon-sdk-java.version}</version>
        </dependency>

        <!-- Canon SDK Java Binding (JNA bindings, pulled transitively but explicit is safer) -->
        <dependency>
            <groupId>org.blackdread</groupId>
            <artifactId>camera-binding</artifactId>
            <version>${canon-sdk-java.version}</version>
        </dependency>

        <!-- JSON serialization for HTTP responses -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Compile with Java 11 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>

            <!-- Build fat JAR with all dependencies -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.photobooth.camera.CameraServerApp</mainClass>
                                </transformer>
                            </transformers>
                            <finalName>canon-driver</finalName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
